/**
 * Warm-up 02 — useDebouncedValue
 *
 * Return a debounced copy of `value` that only updates after it's been stable for
 * `delay` ms. See TASK.md. Stub keeps things compiling (it returns the value with no
 * debouncing, so the timing tests stay red until you implement it).
 */

export function useDebouncedValue<T>(value: T, delay: number): T {
  // TODO: implement (see TASK.md)
  return value;
}
