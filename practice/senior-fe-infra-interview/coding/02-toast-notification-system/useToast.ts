/**
 * Exercise 02 — useToast
 *
 * Consume the toast context and return the imperative API. Throw a clear error if
 * called outside a <ToastProvider>. See TASK.md.
 */

import type { ToastContextValue } from "./types";

export function useToast(): ToastContextValue {
  // TODO: implement (see TASK.md)
  throw new Error("Not implemented");
}
