/**
 * Drill 3 — sanitizeHtml. See TASK.md.
 * Stub returns "" so tests fail on behavior, not imports.
 */

export interface SanitizeOptions {
  allowedTags?: string[];
  allowedAttrs?: string[];
}

export function sanitizeHtml(dirty: string, opts?: SanitizeOptions): string {
  // TODO: implement (see TASK.md)
  return "";
}
