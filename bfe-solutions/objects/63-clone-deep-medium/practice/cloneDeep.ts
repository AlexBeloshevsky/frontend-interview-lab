/**
 * 63. create _.cloneDeep() — medium
 * https://bigfrontend.dev/problem/create-_.cloneDeep
 *
 * Implement a `cloneDeep()` that deeply clones a value. Support primitives,
 * arrays, plain objects, and circular references (a fully recursive clone, where
 * mutating the copy never affects the original).
 *
 *   const original = { a: 1, b: { c: [1, 2, 3] } }
 *   const copy = cloneDeep(original)
 *   copy.b.c.push(4)        // original.b.c is unchanged
 */

export function cloneDeep<T>(data: T): T {
  // TODO: implement
  throw new Error("Not implemented");
}
